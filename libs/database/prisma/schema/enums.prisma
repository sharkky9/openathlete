enum UserRole {
  ATHLETE
  COACH

  @@map("user_role")
}

enum Gender {
  MALE
  FEMALE
  OTHER

  @@map("gender")
}

enum EventType {
  TRAINING
  COMPETITION
  NOTE
  ACTIVITY

  @@map("event_type")
}

enum ConnectorProvider {
  STRAVA
  GARMIN
  FITBIT
  APPLE_HEALTH
  SUUNTO
  COROS
  POLAR
  INTERVALS_ICU

  @@map("connector_provider")
}

enum SportType {
  RUNNING
  TRAIL_RUNNING
  CYCLING
  SWIMMING
  ROCK_CLIMBING
  HIKING
  STRENGTH
  CROSSFIT
  YOGA
  ALPINE_SKI
  BACKCOUNTRY_SKI
  BADMINTON
  CANOEING
  E_BIKE_RIDE
  ELLIPTICAL
  E_MOUNTAIN_BIKE_RIDE
  GOLF
  GRAVEL_RIDE
  HANDCYCLE
  HIGH_INTENSITY_INTERVAL_TRAINING
  ICE_SKATE
  INLINE_SKATE
  KAYAKING
  KITESURF
  MOUNTAIN_BIKE_RIDE
  NORDIC_SKI
  PICKLEBALL
  PILATES
  RACQUETBALL
  ROLLER_SKI
  ROWING
  SAIL
  SKATEBOARD
  SNOWBOARD
  SNOWSHOE
  SOCCER
  SQUASH
  STAIR_STEPPER
  STAND_UP_PADDLING
  SURFING
  TABLE_TENNIS
  TENNIS
  VELOMOBILE
  VIRTUAL_RIDE
  VIRTUAL_ROW
  VIRTUAL_RUN
  WALK
  WEIGHT_TRAINING
  WHEELCHAIR
  WINDSURF
  WORKOUT
  TRIATHLON
  DUATHLON
  AQUATHLON
  AQUABIKE
  OTHER

  @@map("sport_type")
}

enum TrainingZoneType {
  HEARTRATE
  POWER
  PACE

  @@map("training_zone_type")
}

enum TokenType {
  PASSWORD_RESET
  ATHLETE_INVITATION
  COACH_INVITATION

  @@map("token_type")
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  REJECTED

  @@map("invitation_status")
}

enum RecordType {
  SPEED
  HEARTRATE
  POWER
  ELEVATION_GAIN
  ELEVATION_LOSS
  CADENCE

  @@map("record_type")
}

enum AgentMessageRole {
  USER
  ASSISTANT
  SYSTEM
  TOOL

  @@map("agent_message_role")
}

enum AgentMessageStatus {
  pending
  processing
  completed
  error

  @@map("agent_message_status")
}

enum AgentBlockType {
  TEXT
  THINKING
  TOOL_CALL
  TOOL_RESULT
  CODE
  CHART_LINE
  CHART_BAR
  CHART_PIE
  CHART_SCATTER
  CHART_AREA
  TABLE
  MAP
  ACTIVITY_SUMMARY
  ACTIVITY_LIST
  ACTIVITY_CREATED
  TRAINING_PLAN
  ERROR
  IMAGE
  FILE

  @@map("agent_block_type")
}

enum AgentBlockStatus {
  pending
  processing
  completed
  error
  cancelled

  @@map("agent_block_status")
}

enum EquipmentType {
  SHOE
  BIKE

  @@map("equipment_type")
}

enum NormalizationFactor {
  SLOPE
  TEMPERATURE
  ALTITUDE
  WIND
  HUMIDITY
  RADIATION

  @@map("normalization_factor")
}

enum MetricType {
  // Santé & composition corporelle
  WEIGHT
  HEIGHT
  BMI
  BODY_FAT
  MUSCLE_MASS
  BONE_MASS
  BODY_WATER
  DAILY_CALORIES
  DAILY_ACTIVE_CALORIES
  DAILY_BMR_CALORIES
  DAILY_STEPS
  DAILY_DISTANCE
  DAILY_ACTIVE_MINUTES
  DAILY_MODERATE_MINUTES
  DAILY_VIGOROUS_MINUTES
  DAILY_FLOORS
  SLEEP_DURATION
  SLEEP_SCORE
  SLEEP_REM_DURATION
  SLEEP_DEEP_DURATION
  SLEEP_LIGHT_DURATION
  SLEEP_AWAKE_DURATION
  SLEEP_RESPIRATION_AVG
  SLEEP_SPO2_AVG
  NAP_DURATION
  HOOPER_INDEX

  // Cardiaque
  HR_MAX
  HR_REST
  HR_MIN_DAILY
  HR_AVG_DAILY
  HR_MAX_DAILY
  HR_RESERVE
  RMSSD
  HRV_LAST_NIGHT_AVG
  HRV_LAST_NIGHT_5MIN_HIGH
  SNAPSHOT_HEART_RATE_AVG
  SNAPSHOT_STRESS_AVG
  SNAPSHOT_RESPIRATION_AVG
  SNAPSHOT_SPO2_AVG
  SNAPSHOT_SDNN
  SNAPSHOT_RMSSD
  RESPIRATION_RATE_AVG
  PULSE_OX_AVG
  PULSE_OX_MIN
  BLOOD_PRESSURE_SYSTOLIC
  BLOOD_PRESSURE_DIASTOLIC
  BLOOD_PRESSURE_PULSE
  STRESS_AVERAGE
  STRESS_MAX
  STRESS_DURATION
  STRESS_REST_DURATION
  STRESS_ACTIVITY_DURATION
  STRESS_LOW_DURATION
  STRESS_MEDIUM_DURATION
  STRESS_HIGH_DURATION
  BODY_BATTERY_CHARGED
  BODY_BATTERY_DRAINED
  SKIN_TEMP_DEVIATION

  // Performance & physiologie
  VMA
  CRITICAL_POWER_RUNNING
  CRITICAL_POWER_CYCLING
  VO2MAX
  VO2MAX_CYCLING
  FTP_RUNNING
  FTP_CYCLING
  VERTICAL_SPEED_AVG
  VERTICAL_SPEED_MAX
  FITNESS_INDEX
  FITNESS_AGE

  @@map("metric_type")
}

enum TrainingLoadCalculationType {
  FOSTER_RPE // Foster method using RPE x duration
  TRIMP // TRIMP using exponential HR weighting

  @@map("training_load_calculation_type")
}

enum WorkoutStepType {
  WARMUP
  COOLDOWN
  INTERVAL_ACTIVE // work interval
  INTERVAL_REST // recovery interval
  STEADY // steady pace/effort
  REPEAT // container for repeated steps
  FREE // open/unstructured

  @@map("workout_step_type")
}

enum WorkoutDurationType {
  TIME // duration in seconds
  DISTANCE // distance in meters
  CALORIES // calories to burn
  HR_BELOW // until HR drops below value
  HR_ABOVE // until HR rises above value
  LAP_BUTTON // manual lap press
  OPEN // no defined duration
  REPS // number of repetitions (for strength training)

  @@map("workout_duration_type")
}

enum WorkoutTargetType {
  OPEN // no specific target
  PACE // pace/speed target (stored as m/s, can display in min/km)
  HEARTRATE // heart rate target
  POWER // power target
  CADENCE // cadence target
  RPE // rate of perceived exertion
  WEIGHT // weight/load target (for strength training)
  ZONE // training zone (1-5 typically for HR/power zones)

  @@map("workout_target_type")
}

enum ProviderAccountStatus {
  active
  revoked

  @@map("provider_account_status")
}

enum ProviderWorkoutExportStatus {
  pending
  success
  failed
  skipped

  @@map("provider_workout_export_status")
}

enum PlanStatus {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED

  @@map("plan_status")
}

enum CyclePhase {
  BASE
  SPECIFIC
  TAPER
  RECOVERY
  COMPETITION

  @@map("cycle_phase")
}

enum AvailabilityPriority {
  LOW
  MEDIUM
  HIGH

  @@map("availability_priority")
}

enum UserLanguage {
  FR
  EN

  @@map("user_language")
}

enum ActivitySegmentType {
  LAP // automatic or manual lap from device
  WORKOUT_STEP // segment from a planned workout
  MANUAL // manually created segment
  AUTO_SPLIT // automatically detected split (e.g., by distance)

  @@map("activity_segment_type")
}

enum InjuryStatus {
  WORSENING
  IMPROVING
  STABLE
  RESOLVED

  @@map("injury_status")
}

enum SubscriptionPlan {
  FREE
  ATHLETE_PRO
  COACH_PRO
  COACH_ULTRA
  CLUB_PRO
  CLUB_ULTRA

  @@map("subscription_plan")
}

enum SubscriptionStatus {
  active
  canceled
  past_due
  trialing
  incomplete
  incomplete_expired
  unpaid

  @@map("subscription_status")
}
